dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install google-chrome-stable
machine: # 虛擬機的環境
  java:
    version: openjdk8
  node:
    version: 7.10.0 
workflows:
  version: 1
  build:
    jobs:
      - npm run build